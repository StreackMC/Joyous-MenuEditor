# 依赖目录
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
lerna-debug.log*

# 依赖锁定文件 (如果使用锁定文件，通常需要提交，但如果你不希望提交，可以取消注释)
# package-lock.json
# yarn.lock
# pnpm-lock.yaml

# 操作系统生成文件
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
desktop.ini

# 编辑器/IDE (根据你使用的 IDE 选择取消注释)
# VSCode
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
*.code-workspace

# IntelliJ IDEA / WebStorm
.idea/
*.iws
*.iml
*.ipr
*.sw?

# Vim
*.swp
*.swo
*~
.DS_Store

# 运行时数据
*.pid
*.seed
*.pid.lock

# 日志文件
logs
*.log

# 测试覆盖率报告
coverage/
*.lcov
.nyc_output/

# 构建产物
dist/
build/
out/
.next/
.nuxt/
.cache/

# 环境变量文件 (通常包含密钥，绝对不要提交)
.env
.env.local
.env.*.local
.env.production
.env.test

# 临时文件
tmp/
temp/

# 配置文件 (如果你不希望提交某些配置文件)
# 例如：各种应用的 config.json
config/
!.env.example

# GitHub Actions 工作流 (注意：这是为了确保你不意外忽略掉工作流文件夹本身)
# 通常 .github/workflows 目录需要保留在 git 中，所以这里明确让它不被忽略
!.github/workflows/

# 其它可能存在的文件
*.tsbuildinfo
.eslintcache
.prettiercache